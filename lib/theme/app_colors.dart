import 'package:flutter/material.dart';

/// Cappuccino Color Palette for Rider App
class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  static const lightLatte = Color(0xFFD6B588);      // Login card
  static const creamGrey = Color(0xFFC6C0B9);       // Background
  static const coffeeBrown = Color(0xFF705E46);     // Secondary elements
  static const darkEspresso = Color(0xFF422701);    // Primary button & text
}
