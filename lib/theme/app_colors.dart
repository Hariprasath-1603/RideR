import 'package:flutter/material.dart';

/// Cappuccino Color Palette for Rider App
class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  static const lightLatte = Color(0xFFD6B588);      // Login card
  static const creamGrey = Color(0xFFC6C0B9);       // Background
  static const coffeeBrown = Color(0xFF705E46);     // Secondary elements
  static const darkEspresso = Color(0xFF422701);    // Primary button & text
  
  // Modern Booking Screen Colors
  static const primaryGreen = Color(0xFF00C853);    // Primary CTA button
  static const darkGreen = Color(0xFF00A844);       // Hover state
  static const accentBlue = Color(0xFF2962FF);      // Secondary accent
  static const lightBlue = Color(0xFFE3F2FD);       // Light backgrounds
  
  static const pureWhite = Color(0xFFFFFFFF);       // Cards, containers
  static const lightGrey = Color(0xFFF5F5F5);       // Subtle backgrounds
  static const mediumGrey = Color(0xFFBDBDBD);      // Borders, dividers
  static const darkGrey = Color(0xFF424242);        // Secondary text
  static const almostBlack = Color(0xFF212121);     // Primary text
  
  static const warningRed = Color(0xFFD32F2F);      // SOS, alerts
  static const successGreen = Color(0xFF43A047);    // Success states
  static const infoBlue = Color(0xFF1976D2);        // Info elements
  
  // Transparent overlays
  static const shadowColor = Color(0x1A000000);     // 10% black
  static const overlayColor = Color(0x33000000);    // 20% black
  
  // Premium Dark Theme Colors
  static const premiumBlack = Color(0xFF0A0A0A);    // Deep black background
  static const darkCharcoal = Color(0xFF1A1A1A);    // Card backgrounds
  static const softGray = Color(0xFF2A2A2A);        // Elevated surfaces
  static const midGray = Color(0xFF757575);         // Secondary text
  static const lightGray = Color(0xFFE0E0E0);       // Primary text on dark
  
  static const premiumGold = Color(0xFFFFB300);     // Primary accent (yellow/gold)
  static const brightYellow = Color(0xFFFFC107);    // Bright yellow accent
  static const darkGold = Color(0xFFFF8F00);        // Pressed state
  
  static const luxuryWhite = Color(0xFFFAFAFA);     // Pure white for light screens
  static const cardWhite = Color(0xFFFFFFFF);       // Card backgrounds
  
  // Gradient colors
  static const gradientStart = Color(0xFF000000);   // Black
  static const gradientEnd = Color(0xFF1A1A1A);     // Dark gray
}
